/**
 * @author Paul Scott
 * @version 25 January 2019
 *
 * class representing a point and its angle
 */
class PointAngle {

    constructor(x, y, angle, inHull) {
        this.x = x;
        this.y = y;
        this.angle = angle;
        this.inHull = inHull;
    }
}